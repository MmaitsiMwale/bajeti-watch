[pytest]
testpaths = tests/ingestion
addopts = -v --tb=short
markers =
    local: Tests that run with no external services (pure Python)
    integration: Tests that require Supabase and/or Groq credentials